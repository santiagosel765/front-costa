import { Routes } from '@angular/router';

import { InventoryShellComponent } from './inventory-shell.component';

export const INVENTORY_ROUTES: Routes = [
  { path: '', component: InventoryShellComponent },
];
